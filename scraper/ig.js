/**
 * ig.js — stub
 * File asli belum disertakan. Instagram akan fallback ke Savefbs di aio.js.
 */
export default async function instagramDownloader(url) {
  throw new Error("ig.js asli belum tersedia. Gunakan Savefbs fallback.");
}
